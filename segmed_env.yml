name: segmed
channels:
  - defaults
dependencies:
  - pip
  - python=3.7
  - matplotlib
  - pip:
    - tensorflow
    - scikit-image
    - scikit-learn
    - pytest
    - pytype
    - nox
    - .
prefix: /home/edwinb/anaconda3/envs/segmed

