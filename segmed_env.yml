name: segmed
channels:
  - defaults
dependencies:
  - pip
  - python=3.7
  - matplotlib
  - pip:
    - tensorflow
    - scikit-image
    - scikit-learn
    - pytest
    # Install segmed, i.e. this package
    - .
prefix: /home/edwinb/anaconda3/envs/segmed

